{
  "name": "bione-astro",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "node scripts/optimize-assets.mjs && astro build",
    "preview": "astro preview",
    "cf:preview": "wrangler pages dev dist",
    "cf:deploy": "npm run build && wrangler pages deploy dist --project-name healthstrat"
  },
  "devDependencies": {
    "astro": "^5.0.0",
    "sharp": "^0.35.1",
    "wrangler": "^4.93.1"
  }
}
